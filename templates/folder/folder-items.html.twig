{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
        <div class="btn btn-primary">
            {{ folder.name }} <span class="badge badge-light">
            {% if itemsCount > 0 %}    
                {{ itemsCount }}
            {% else %}
                Vacia
            {% endif %}    
            </span>
        </div>
        <a href="#" class="btn btn-warning">Editar</a>
        <a href="#" class="btn btn-success">Nuevo</a>
        {{ include('item/_item-list.html.twig', { 'items': items }) }}
    </div> 
{% endblock %}
