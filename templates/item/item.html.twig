<div class="card">
    <div class="card-body">
        <a href="{{ path('items-by-user', {'username' : item.user.username } ) }}">
            <small>{{ item.user.username }}</small>
        </a>
        <h5 class="card-title">{{ item.title }}</h5>
        <p class="card-text">{{ item.body }}</p>
        <a href="" class="btn btn-primary">Ver nota</a>
    </div>
</div>   