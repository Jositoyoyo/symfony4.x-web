{% extends 'base.html.twig' %}
{% block body %}

<main role="main" class="content">

      <div class="container">
        <!-- Example row of columns -->
        <div class="row">
         
{% for note in notes %}
        {{ include('note/item-list.html.twig', { 'note': note }) }}
    {% endfor %}
        </div>

        <hr>

      </div> 

    </main>

{% endblock %}
