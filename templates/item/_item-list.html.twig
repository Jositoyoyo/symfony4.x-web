{% if items %}
    {% for item in items %}
        {{ include('item/_item.html.twig', { 'item': item }) }}
    {% endfor %} 
{% else %}
    {{ include('components/alert/_alert-warning.html.twig', { 'msg': 'No se han encontrado notas' }) }}
{% endif %}    