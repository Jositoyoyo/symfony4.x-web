<div class="card">
  <div class="card-body">
    <h5 class="card-title">{{ user.name }}</h5>
    <p class="card-text">{{ user.lastname }}</p>
    <a href="{{ path('note-view', {'slug': user.username}) }}" class="btn btn-primary">Ver nota</a>
  </div>
</div> 