{% if users %}
    {% for user in users %}
        {{ include('user/user-item.html.twig', { 'user': user }) }}
    {% endfor %} 
{% else %}
    {{ include('components/alert/_alert-warning.html.twig', { 'msg': 'No se han encontrado notas' }) }}
{% endif %}    