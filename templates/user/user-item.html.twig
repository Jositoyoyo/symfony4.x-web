<div class="card">
  <div class="card-body">
    <h5 class="card-title">{{ user.name }}</h5>
    <p class="card-text">{{ user.lastname }}</p>
    <a href="{{ path('user-edit', { 'user' : user.username } ) }}" class="btn btn-primary">Editar</a>
  </div>
</div> 